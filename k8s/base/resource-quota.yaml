apiVersion: v1
kind: ResourceQuota
metadata:
  name: liberu-crm-quota
  namespace: liberu-crm
  labels:
    app: liberu-crm
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "8"
    limits.memory: 8Gi
    pods: "20"
    services: "10"
    persistentvolumeclaims: "10"
