apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: liberu-crm-storage
  namespace: liberu-crm
  labels:
    app: liberu-crm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
